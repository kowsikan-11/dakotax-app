/* Optional: hard-wire the sheet link into the site so no device has to paste it.
 *
 * Uncomment the line below and put your own /exec URL in it, then commit.
 * Leave it as-is to let each device connect itself from the welcome screen.
 *
 * Anyone who can open the site can then read and write the sheet, which is the
 * trade-off you accept by deploying the script with access set to "Anyone".
 */
// window.DAKOTAX_API_URL = 'https://script.google.com/macros/s/XXXXXXXXXXXX/exec';
